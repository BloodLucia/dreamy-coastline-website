---
interface Props {
  title: string;
  hasMore?: boolean;
}

const { title, hasMore = false } = Astro.props
---

<section class="border-b pb-8">
  <h2 class="mb-4 text-3xl">{title}</h2>
  <slot />
</section>