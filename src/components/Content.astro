---
interface Props {
  className?: string
}

const { className }= Astro.props
---

<article class:list={[className, "md:max-w-[640px] h-full overflow-y-auto mx-auto max-md:px-4 mb-12 flex flex-col gap-12"]}>
  <slot />
</article>